#include "main.h"

/**
 * mul - pultiplies two numbers
 * @a: the first number to multiply
 * @b: the second number to multiply with the first
 * Return: the result of the multuiplication between a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
